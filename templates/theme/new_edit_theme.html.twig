{% extends 'base.html.twig' %}

{% block title %}Ajouter une Categorie{% endblock %}

{% block body %}
<div class="container">

    {% if edit %}
        <h1>Modifier le theme: {{ theme.label }}</h1>    
    {% else %} 
        <h1>Ajouter un nouveau theme</h1>
    {% endif %} 
        
    {{ form_start(formNewTheme) }}
        
        {{ form_row(formNewTheme.label) }}
        
    {{ form_end(formNewTheme) }}
</div>
{% endblock %}

